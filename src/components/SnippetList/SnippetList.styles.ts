import type { SxThemeProps } from '@/types/sx.types';

export const snippetListStyles = {
  container: {
    width: '100%',
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'column',
    gap: 5,
  },
} satisfies SxThemeProps;
